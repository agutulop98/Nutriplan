apiVersion: v1
kind: Service
metadata:
  name: nutriplan-frontend
  labels:
    app: nutriplan-frontend
spec:
  selector:
    app: nutriplan-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP

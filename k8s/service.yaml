apiVersion: v1
kind: Service
metadata:
  name: nutriplan-backend
  labels:
    app: nutriplan-backend
spec:
  selector:
    app: nutriplan-backend
  ports:
    - name: http
      port: 80
      targetPort: 3001
  type: ClusterIP
